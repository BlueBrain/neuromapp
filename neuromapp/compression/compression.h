#ifndef MAPP_COMPRESSION_EXECUTE_
#define MAPP_COMPRESSION_EXECUTE_ 
// todo have another file in the directory that could have a function called by the execute so that I don't have to have the function in the main.cpp
namespace {
    void hello_zlib();
}
//above is taken from the zlib file

int comp_execute(int argc,char *const argv[]);
#endif /* ifndef MAPP_COMPRESSION_EXECUTE_ */
